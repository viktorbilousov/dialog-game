package minigames.mail.phrase

import models.Answer
import models.items.phrase.FilteredPhrase

class MailPhase(id: String, phrases: Array<String>, answers : Array<Answer>) : FilteredPhrase(id, phrases, answers){


}